<div
  class="{{classnames 'section' footer=footer openings=jobs spotify=spotify }}"
  {{#if hideable}} mc:hideable{{/if}}
  {{#if variant}} mc:repeatable="body" mc:variant="{{variant}}"{{/if}}
>
  {{> @partial-block}}
</div>
